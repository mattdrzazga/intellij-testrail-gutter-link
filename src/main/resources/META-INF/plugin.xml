<idea-plugin>
    <id>com.mateo.plugins.testraillink</id>
    <name>TestRail Gutter Link</name>
    <vendor url="https://github.com/mattdrzazga/intellij-testrail-gutter-link">Mateusz Drzazga</vendor>

    <description><![CDATA[
        Adds a gutter icon next to every <code>@TestCase(testCaseId = "...")</code>
        annotation in Java and Kotlin test sources. Clicking the icon opens the
        matching TestRail case in your default browser.<br/>
        <br/>
        <b>Configuration.</b> Set your TestRail base URL per project under
        <b>Settings &rarr; Tools &rarr; TestRail Gutter Link</b>. The plugin opens
        <code>&lt;base-url&gt;/index.php?/cases/view/&lt;id&gt;</code> &mdash; for
        example, <code>https://your-company.testrail.io/index.php?/cases/view/12345</code>.<br/>
        <br/>
        <b>Matching.</b> Any annotation whose simple name is <code>TestCase</code>
        and that is applied to a method &mdash; regardless of its package &mdash;
        is recognized. Only files under test source roots
        (e.g. <code>src/test</code>, <code>src/androidTest</code>) get the icon.
        Works with both the K1 and K2 Kotlin compiler modes.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider
            language="JAVA"
            implementationClass="com.mateo.plugins.testraillink.TestCaseLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider
            language="kotlin"
            implementationClass="com.mateo.plugins.testraillink.TestCaseLineMarkerProvider"/>
        <projectConfigurable
            parentId="tools"
            id="com.mateo.plugins.testraillink.settings"
            displayName="TestRail Gutter Link"
            instance="com.mateo.plugins.testraillink.TestRailSettingsConfigurable"/>
    </extensions>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>
</idea-plugin>
